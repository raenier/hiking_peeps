<%= form_for @comment, as: :comment, url: comments_path(@comment.commentable), data: { controller: "form", action: "turbo:submit-end->form#reset" } do |f| %>
  <%= f.text_area :body %>
  <%= f.submit "COMMENT", class: "comment_submit" %>
<% end %>
